# Run artifacts (workspaces, PDFs, agent-generated code)
runs/

# Python
__pycache__/
*.egg-info/
.venv/
dist/
.pytest_cache/
.ruff_cache/

# Local per-user config
arxiv-repro.toml

# Secrets
.env
.coverage
coverage.xml

# LaTeX build artifacts (CI uploads the PDF as a workflow artifact)
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/main.pdf
